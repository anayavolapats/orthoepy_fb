<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <group>
        <item
            android:id="@+id/app_bar_words"
            android:icon="@mipmap/ic_words"
            android:title="@string/words_title"
            app:showAsAction="always" />

        <item
            android:id="@+id/app_bar_settings"
            android:title="@string/settings_title"
            android:icon="@mipmap/ic_settings"
            app:showAsAction="always"/>
    </group>

    <group>
        <item
            android:id="@+id/app_bar_study"
            android:icon="@mipmap/ic_study"
            android:title="@string/education_title"
            app:showAsAction="always"/>

        <item
            android:id="@+id/app_bar_user"
            android:title="@string/user_title"
            android:icon="@mipmap/ic_user"
            app:showAsAction="always"/>
    </group>




</menu>